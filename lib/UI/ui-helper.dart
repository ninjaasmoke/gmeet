import 'package:flutter/material.dart';

class UIHelper {
  // colors
  Color bgColor = Color(0xff181818);
  Color accentColor = Color(0xff1f8456);
  Color textColor = Colors.white;
  Color cancelRed = Colors.red;
  Color altRed = Colors.red[300];
  Color buttonBlue = Color(0xff8ab2f8);
  Color iconGrey = Colors.grey;

  // font sizes
  double fontSize = 15.0;
  double headingFontSize = 22.0;
}

// $greenColor: rgb(46, 164, 79);
